name: Apply v13.37 combat framing
on:
  push:
    paths:
      - '.github/workflows/apply-v1337.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/patch-v1337.py
      - run: |
          git config user.name "Gunpowder"
          git config user.email "38918114+gunpowderstudios@users.noreply.github.com"
          git add js/scene3d.js index.html
          git commit -m "Centre and tighten desktop combat framing v13.37"
          git push
