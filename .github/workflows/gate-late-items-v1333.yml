name: Gate late items v13.33
on:
  push:
    paths:
      - '.github/workflows/gate-late-items-v1333.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: python .github/scripts/gate_late_items_v1333.py
      - name: Commit TEST v13.33
        run: |
          git config user.name "Gunpowder"
          git config user.email "38918114+gunpowderstudios@users.noreply.github.com"
          git add js/game.js index.html
          git commit -m "Gate Ice Staff and Large Steel Axe until tile 20 v13.33"
          git push
